resize
clear

dernier constructor vector (copy)
operator = pour iterator
operator != pour iterator
operator + / - pour iterator avec un iterator (git julia)



check cas erreur assign


#include <iostream>
#include <vector>

using namespace std;

int main()
{
    vector<int> test;
    test.push_back(1);
    test.push_back(11);
    test.push_back(111);
    test.push_back(1111);
    vector<int> test2;
    test2.assign(test.begin()+ 1, test.end());
    test2.assign(test.end()+ 1, test.begin());
    //test2.assign(3,5);
    
    cout << "Vector: ";
    vector<int>::iterator iter = test.begin();
    for(iter; iter < test.end(); iter++)
          {
            // access value in the memory to which the pointer
            // is referencing
            cout << *iter << " ";
          }

    cout << "\nVector 2: ";
    iter = test2.begin();
    for(iter; iter < test2.end(); iter++)
          {
            // access value in the memory to which the pointer
            // is referencing
            cout << *iter << " ";
          }

    return 0;
}
